#include <stdio.h>
/**
 * main -program that prints a line with puts function
 * Return: 0 ( it ends the main function to 0 when success)
 */
	int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
	}
